# Example Caddy configuration (automatic HTTPS via Let's Encrypt).
# Adapt and copy this file to /etc/caddy/Caddyfile on the Raspberry Pi.
# Caddy must be the only service exposed on your router's ports 80/443.

mc.yourdomain.tld {
    reverse_proxy 127.0.0.1:8000
}
